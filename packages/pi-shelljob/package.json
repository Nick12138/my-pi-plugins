{
  "name": "pi-shelljob",
  "version": "0.1.0",
  "description": "Background shell jobs for pi: submit long-running commands (builds, installs, dev servers, batch jobs) without blocking the session. Auto-notify on completion, cross-session job store, process-tree kill, tail logs. Tools: shelljob, shell_wait.",
  "keywords": ["pi-package"],
  "license": "MIT",
  "author": "Nick12138",
  "type": "module",
  "pi": {
    "extensions": ["./extensions"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
